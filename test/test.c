#include "array/array.h"

#include <stdio.h>
#include <assert.h>

#define INTV(I) *(int *) I


int main(void)
{
    /*
    Array *arr = array_new(NULL, sizeof(int), 10);
    int value = 32;
    array_fill(arr, &value);

    for (size_t i = 0; i < array_size(arr); ++i) {
        printf("%d\n", *(int *) array_get(arr, i));
    }

    array_clear(arr);
    */


    assert(!arr->members);

    return 0;
}